spring:
  application:
    # 配置文件的名称
    name: application
  cloud:
    config:
      # 配置文件的针对的环境
      profile: test
      # 配置文件所在的仓库的分支
      label: master
      # 配置中心地址
      uri: http://localhost:9001/
      # 访问配置中心的用户名
      username: triumphxx
      # 访问配置中心的密码
      password: 123
server:
  port: 9002
